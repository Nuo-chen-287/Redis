# Redis 专题

## 写在前面的话

本文用于研究 Redis 作为后端缓存中间件的相关应用，文章的前半部分Link ：

> https://zhuanlan.zhihu.com/p/2048179776741979953

如文章有不足之处，欢迎在评论区补充

> 另外，工程的研究没有终点，后端工程作为一个存在了数十年的领域，分支与深入技术无法估量，本文也只是以作者个人学习的角度来分享一下学习过程与相关技术。

本文所用代码：



## V6：RDB & AOF

### 引入

在前面的V1～V5 中，我们成功的吧缓存领域一个个常见的问题都解决

- 请求太多打到 MySQL：加缓存。
- 不存在的数据绕过缓存：处理缓存穿透。
- 热点 key 突然过期：处理缓存击穿。
- 大量 key 同时过期：处理缓存雪崩。
- 多个服务同时重建缓存：使用分布式锁。

但是，这些都依赖一个前提，那就是 Redis 进程始终活着！

假设，我们现在在Redis 中刚刚执行了一条命令

``` mysql
SET product:1001 '{"stock": 99}'
```

Redis 很自然的返回给我们了一句 **OK** ，这句OK的意思是：

> Redis 进程已经把这份数据写进内存，可以读取了。

它并不代表着数据进入了磁盘，或者说，如果Redis 此时进程被杀死，那么内存里的数据就可能全部丢失

这就是我们V6要解决的问题：

> Redis 如何把内存中的数据保存到磁盘，并在重启时重新恢复？

### 思路

那么，如何报内存中的数据保存下来呢？最容易想到的方式就是 “保存结果”，这四个字不仅仅适用于 Redis 中的数据，在工业中，也有很多场景同样适用，比如，我们的项目用到了MySQL数据库，那么白天的工作时间，用户会经常访问我们的Program 也是数据库压力最大的时候，晚上用户都要休息了，很少量的访问导致数据库的压力变小，为了防止一些风险，很多时候就会吧MySQL的数据库做一个备份，存储到OSS上，这样做有两个好处：

1. 给数据安全加一把锁：MySQL在服务器上，万一服务器坏了，MySQL中的数据直接找不回来了。
2. 给数据分析留下方便：很多时候，虽然线上数据库是最新的数据，但是我们内容做数据汇总，数据的业务分析的时候，不需要用到那么新的数据，而且，直接连线上数据库不仅有加大数据库压力的弊端，还有可能会不小心执行“写”操作，这时一个离线的，可恢复的，随便“折腾”的数据库，就给数据分析的场景留下了便利

回到Redis 的场景中，保存结果就使用到了 Redis Database ，简称RDB

初次之外，还有一种方式，也能恢复内存中的数据，就是：“保存过程”，比如我们执行了这些命令：

> SET product:1001 stock=100
> DECR product:1001
> SET order:A paid

那么，能吧Redis 中执行的这些命令保存下来，肯定也能还原内存中的数据，也就是AOF

### 机制

好了，回到主线，来梳理一遍RDB与AOF

首先说，V6 要解决的问题是：

> 保存什么？什么时候保存？重启时怎样恢复？

RDS和AOF就是针对这三个问题，给出两套解决方案



先说RDS，RDS保存的是某个时间点的完整数据快照，比如：

```
10:00 拍摄 RDB：

product:1001 -> stock=100
product:1002 -> stock=50
session:A    -> logged-in
```

最终会生成一个 dump.rdb 文件，这个文件可以用来恢复10:00 时刻的数据

那么， 何时来保存呢？这里有两种方式： 手动触发和根据配置自动触发

手动触发：

+ SAVE：主线程直接生成快照，期间无法正常处理请求，测试环境和toy场景使用。
+ BGSAVE：创建子进程生成快照，主进程继续服务，生产环境使用！

自动触发，比如我们配置：

```
save 3600 1
save 300 100
save 60 10000
```

含义是满足任意条件就执行后台快照，例如“60 秒内至少修改 10000 次”。

BGSAVE的过程是：

```
创建子进程
    ↓
父子进程暂时共享原内存
    ↓
子进程把快照写入临时 RDB
    ↓
主进程继续处理请求
    ↓
写完后原子替换旧 dump.rdb
```



然后是AOF，AOF的重点是，命令在进入Redis之前，先经过AOF缓存区，然后根据不同的策略来决定真正吧命令写入磁盘：

| 策略     | 行为               | 数据风险 & 性能       |
| -------- | ------------------ | --------------------- |
| always   | 每次都写入磁盘     | 风险小 / 性能慢       |
| everysec | 每秒写入磁盘       | 折中                  |
| no       | 交给操作系统来决定 | 风险不可控 / 性能较高 |

这里有一个很重要的 AOF Rewrite ，如果一个计数器，执行了一百万次 INCR，那么保留所有的历史命令会让文件不断变大

但是恢复状态只需要：

```
SET counter 1000000
```

AOF Rewrite 会生成一份能够表达当前状态的精简文件：

```
大量历史写命令
       ↓
BGREWRITEAOF
       ↓
最小化的当前状态
```

它不是修改旧文件，而是后台创建新文件，完成后再替换。



两个机制都讲完了，现在来个小结：

|      | RDB                                                  | AOF                                                          |
| ---- | ---------------------------------------------------- | ------------------------------------------------------------ |
| 优点 | 文件紧凑、恢复速度快、适合备份，持续磁盘开销较低。   | 数据丢失窗口较小，恢复内容通常比 RDB 更新。                  |
| 缺点 | 丢失窗口可能较大；生成快照时需要 fork 和额外内存。   | 持续产生磁盘 I/O，文件更大，需要 Rewrite，恢复增量命令有成本。 |
| 适合 | 普通缓存、允许丢失一段时间数据、重视恢复速度的场景。 | Session、计数器、排行榜等不希望丢失较长时间数据的场景。      |

## V7：主从复制 --- 哨兵机制

### 引入 & 思路

V6 站在技术的角度，解决了Redis 挂掉之后的数据丢失问题，在产品的角度来说，一个Redis 挂了，直接就让我们的用户不能用了么？这当然不行，因此，V7是用来解决当前Redis 进程挂了，如何让服务不挂的问题。我们很容易就能想到，一个服务挂了，新起一个服务不就好了，我们在之前的章节中也提到过，只起一个Redis 服务本身也有内存压力，或者是不同业务下的区分，新起一个服务的成本一定是在立项之初就计算在内的，但是仔细想想，“新起服务” 该如何做呢？

首先说，新起一个服务，不应该是看到挂了再新起，我们看下面一个场景：

```
Redis生产进程 挂了 -> Redis备用进程 启动 -> Redis 备用进程 存入数据 -> Redis备用进程 正式启通
```

这个过程看似很合理，其实有一个非常明显的漏洞：

>  **启动**和**存入数据**都需要时间，还是有一小段窗口内，用户无法使用给我们的服务

也就是说，这个方案有待改进，不然我们的根本问题未被解决，至此主从复制的方案上桌！

### 机制

我们说，等前面一个Redis 服务挂了，再来新启动一个进程，肯定是不合理的，更好的方案是：

> 提前运行第二个Redis进程，并让他持续复制第一个Redis 进程的数据，并在第一个进程挂掉后，尽可能快的接上，保证服务不挂。

也就是说：

``` 
客户端写入 Redis A
          ↓
Redis A 修改数据
          ↓
Redis A 把变化同步给 Redis B
          ↓
Redis B 保持相近的数据
```

例如库存最开始是 `100`：

```
Redis A：inventory = 100
Redis B：inventory = 100
```

用户购买一件：

```
客户端向 Redis A 执行 DECR
```

随后：

```
Redis A：inventory = 99
Redis B：inventory = 99
```

这就是主从复制！

我们把问题明确一下， 主节点Redis 挂了，副节点的Redis “接班” 的整个过程就做三件事：

1. 判断故障 --- 判断Redis 进程是否真的挂了
2. 选择并提升新主 --- 把某个副本提升为新主
3. 让客户端找到新主 --- 告诉客户端新主在哪里

这三件事情，每个都很重要，下面我来介绍一下其中的链路

#### 判断故障

第一件事：如何判断Redis 进程真的挂了。

Sentinel 会定期向主节点发送类似 `PING` 的探测：

```
Sentinel ──PING──→ 主节点
Sentinel ←─PONG── 主节点
```

如果得不到回应，这个Sentinel就会认为：

> **在我看来**，主节点已经挂了

这个状态叫主观下线，**SDOWN**，也就是 Subjectively Down。

值得注意的是，为什么要有强调**主观**，那是因为，Sentinel没有得到回应，可能是Sentinel自身的问题：

比如 Sentinel 在Ping 的过程中，网络中断了，所以我们不能只设置一个 Sentinel 来完成任务

常用的方式是，设置多个Sentinel ，用一个成功率来判断主节点是否正常，这个阈值叫做 quorum，也就是说，有quorum 个哨兵同时主观看到主节点已经挂了，我们才认为，主节点真的挂了，到下一个阶段。

#### 选择并提升新主

上一个阶段中，我们判断出来主节点挂了，那么，如何吧备用的节点替换到主节点的位置，是本任务的核心。

这里分为三步来吧这个任务完成：

> 第一步：选出一个哨兵作为leader，它来负责执行
>
> 第二步：选择一个合适的副本
>
> 第三步：真正提升副本

假设有三个哨兵同时认为主节点挂了：

```
Sentinel1: 确认
Sentinel2: 确认
Sentinel3: 确认
```

那么，就需要先在这三个哨兵中选择一个临时负责人，这里要用到投票机制：

```
Sentinel1: 确认  --- |
Sentinel2: 确认      | ----> 选择Sentinel2作为临时负责人
Sentinel3: 确认  --- | 
```

> 为什么要选一个临时负责人来执行操作呢？首先说，最好是只有一个点来操作，不然多个点同时操作会让“操作”的流程变乱
>
> 其次，如果有两个负责人的话，它们的意见可能会不一直： B 和 C节点同时被选中了，不知道该听哪个负责人的。

接下来， Sentinel2 会在副本中选择一个最合适的，比如，上次恢复到现在：

```
主节点(已挂)： 保留了 1000 条命令
副本A： 保留了800 条命令
副本B： 保留了1000条命令  -> 被负责人选中
副本C： 保留了600 条命令
```

最后，负责人会讲副本B提升至主节点，吧主节点降级下来变成副节点，也就是执行：

```
REPLICAOF NO ONE
```

这条命令的含义是：

> 不再做任何节点的副本，从现在开始成为独立主节点。

同时，把副本A和C从复制之前的主节点改为复制现在的副本B。

总结下来，这里的链路就是：

```
旧主被判定 ODOWN
        ↓
Sentinel 选出一个 Leader
        ↓
Leader 选择最合适的副本 B
        ↓
对 B 执行 REPLICAOF NO ONE
        ↓
B 成为新主
        ↓
其他副本改为复制 B
```

#### 让客户端找到新主

上一个阶段，我们吧已经挂掉的进程节点降级，把一个最合适的副本升级为主节点，还有一个问题没有解决：Client 端不认识你这个新的节点

假设最初：

```
主节点 A：6379
副本 B：6380
```

客户端一直连接 `6379`。

后来 A 挂了，Sentinel 把 B 提升为新主：

```
旧主 A：6379，已下线
新主 B：6380，正常工作
```

Redis 集群内部虽然切换成功，但客户端仍在访问 `6379`：

```
客户端 ──×──→ 旧主 A:6379

新主 B:6380 正常，但客户端不知道
```

所以，客户端不能写死主节点地址。而是应该改为记录Sentinel，也就是：

``` 
客户端原本连接主节点 A
        ↓
A 挂掉，客户端操作失败
        ↓
Sentinel 完成故障判断和选主
        ↓
副本 B 被提升为新主
        ↓
客户端重新询问 Sentinel
        ↓
Sentinel 返回 B 的地址
        ↓
客户端改为连接 B
```

其实在 Redis 的讨论中，这三个问题也有很高的价值，后续再来更新

1. 主从复制的过程中，异步复制的副本是否永远和主节点一样新？
2. 关于缓存一致性
3. 企业级分层拦截链路
