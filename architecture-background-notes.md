# 架构背景讨论笔记

> 这份文档收纳 Redis 专题学习过程中，那些「不属于某个具体版本、但作为背景必须搞清楚」的架构问题。
> 和 `redis-notes.md`（V1→V7 版本主线）分开放。
> 分三档状态：**🟢 已经吃透**（能自己讲明白、能举一反三）、**✅ 已经初步了解**（建立了心智模型）、**⏳ 待解决**（先记下，专题学完再回来）。

---

## 🟢 已经吃透

### 1. 数据库为什么有连接池上限（`DB_POOL_SIZE = 10`）

- MySQL 实例跑在一台机器上，受这台机器的 **CPU / 内存**限制。
- 每条 DB 连接都要占服务端一份内存 + 一个线程 + 可能的锁/事务上下文，**不是免费的**。
- 不限制的话，1 万并发就开 1 万连接，DB 还没查数据就先被“连接管理”本身压垮。
- 所以连接池主动把并发卡在安全水位（`maxconnections`）。在 V1 里它是**故意制造的瓶颈**，让“DB 扛不住”用肉眼可见的方式显形：
  `50 请求 ÷ 10 连接 × 200ms ≈ 1s`。

### 2. 应用服务器 vs 数据库服务器（关键心智模型）

真实部署里这是**两台（组）不同的电脑**，性格完全不同：

| | 应用服务器（跑 CRUD 代码） | 数据库服务器（跑 MySQL） |
|---|---|---|
| 状态 | **无状态**（数据不在本地） | **有状态**（数据就在它这） |
| 扩容 | ✅ 极容易，加机器即可 | ❌ 极难（要主从/分库分表，贵且复杂） |
| 是不是瓶颈 | 通常不是（不够就加机器） | **通常就是它**（最贵、最难扩、最先倒） |

比喻：应用服务器=**收银员**（不够就多招几个）；数据库=仓库里**唯一那本账本**（再多收银员也得排队翻同一本）。

### 3. 「处理高并发」主要在处理谁？

- **两边都要，但难点 80% 在数据库侧（以及它前面的缓存）。**
- 因为应用服务器无状态、可线性扩容；DB 有状态、难扩、易倒，是真正卡死系统的环节。
- **最有效的第一招就是缓存**：在“账本”前放一个 Redis，让绝大多数请求不用翻账本 → 这正是本专题主线。

### 4. 「加机器」到底是什么 —— 两个扩容方向

| | 垂直扩容 (Scale Up) | 水平扩容 (Scale Out) |
|---|---|---|
| 做法 | 同一台机器换更强 CPU / 加内存 | **加更多台机器一起干** |
| 上限 | 有天花板，且单点 | 几乎无上限，顺便容灾 |
| 我们说的「加机器」 | ❌ | ✅ 就是这个 |

**「加机器」的真相**：把**同一份无状态程序复制多份**，同时部署到多台机器，前面放一个**负载均衡器**（Nginx/LVS/SLB）按规则分发请求。对用户完全无感（只看到一个网址）。
→ 不是把程序拆开放到多台，而是**复制多份 + 前面分流**。

### 5. 缓存的 TTL 由「创建时间」决定，命中不续命

- `cache.r.set(key, ..., ex=60)` 里的 `ex=60` 是**从写入那一刻起的绝对倒计时**，60 秒后准时消失。
- **读取（命中）不会重置 TTL**。真实验证过：写入 TTL=60，命中 2 次、过 4 秒后 TTL=56，没有回到 60。
- 只有**再次写入 / 重新设置过期时间**（如缓存过期后 miss 回源重新 `set`，或显式 `expire`）才会刷新倒计时。
- TTL 是 **key 级别**的，不是 value 内部元素级别的：比如 Set 里的单个成员不能各自单独设置 TTL。
- 另有一套“按最后访问时间淘汰”的机制叫 LRU/LFU（`maxmemory-policy`），那是**内存满了才被动触发**的淘汰，和我们主动设的 TTL 是两码事，别混。
- 关联后续：TTL 到点必死 → 热点 key 到期瞬间会引出「缓存击穿」；大量 key 同时到期会引出「缓存雪崩」；空值缓存要用短 TTL，是在“挡穿透”和“数据新鲜”之间取平衡。

### 6. Redis 的数据类型 / K-V 到底怎么存内容

- Redis 是 **K-V 数据库**：key 是字符串；value 不是只能放字符串，而是可以选择不同的数据结构。所有这些结构主要都在**内存**里操作，所以快。
- **String**：最通用。适合缓存完整对象 JSON（如 `product:1001 -> {"id":1001,...}`）、计数器（`INCR`）、开关标记、空值标记。优点是简单；缺点是对象里改单个字段时通常要整包读写。
- **Hash**：适合“一个对象有多个字段”的场景，如 `product:1001` 下面有 `name / price / stock`。优点是能只读写单个字段；是否比 String+JSON 更合适，取决于是否经常局部更新、局部读取。
- **List**：有顺序、可两端 push/pop。适合消息队列雏形、最近访问记录、阻塞弹出等“按顺序进出”的场景。
- **Set**：无序、去重。适合 UV 去重、黑名单、标签集合、共同好友/交并差集。边界是**成员不能单独设 TTL**。
- **ZSet**：Set + 分数。适合排行榜、按时间戳排序的延迟队列、范围查询。用 score 表达“排名 / 时间 / 权重”。
- 选类型的关键不是“内容长什么样”，而是**访问模式**：整包读写优先 String+JSON；频繁局部字段操作考虑 Hash；去重集合用 Set；排序/范围/延迟触发用 ZSet；顺序进出用 List。

---

## ✅ 已经初步了解

### 1. 加机器 vs 微服务 —— 两个不同的轴

- **加机器（水平扩容）= 复制**：相同的代码 × N 份。回答“这个服务不够扛怎么办”。
- **微服务 = 拆分**：把一个大程序按业务功能拆成多个**不同的**小程序（用户/商品/订单/支付服务），各自独立部署。回答“大程序太臃肿、想各部分独立开发与按需扩容”。
- **两轴叠加**：每个微服务自己再水平扩容（如双 11 只猛加商品+订单服务的副本数）。

### 2. 加机器 / 微服务 vs 分布式架构 —— 包含关系

- **分布式是大伞**：只要系统由“跑在多台机器、通过网络协作”的部分组成，就是分布式。
- 水平扩容、微服务都装在这把伞下面；微服务是分布式更进一步的形态。
- **一旦从 1 台变多台，就已经在搞分布式**（哪怕没拆微服务）。
- 分布式带来的新问题，正好对应本专题后续版本：

| 分布式新问题 | 本专题哪一版治 |
|---|---|
| 多台机器要共享缓存（不能各存各的） | **V2**（缓存放公共 Redis 而非进程内存，就是这原因） |
| 多台机器抢同一资源（如扣同一商品库存） | **V5 分布式锁** |
| 公共组件 Redis 别成单点 | **V6 持久化 / V7 主从哨兵** |

> 一句话总结：「加机器」= 水平扩容 = 把同一份无状态程序复制多份、用负载均衡器分流（≠ 给单机加 CPU/内存）。当你这么干、或把程序按功能拆成微服务时，系统就成了「分布式架构」——而分布式带来的“多台机器如何共享数据、如何协调”这些新问题，正是本 Redis 专题 V2、V5、V7 在逐一解决的。

---

## ⏳ 待解决（先记下，专题学完再回来）

### 1. 企业级「分层拦截链路」里每一层具体做什么

```
用户 → 网关限流 → 本地缓存 → Redis缓存 → 连接池 → DB
       (A)        (B)       (C)        (D)     (E)
```

目前只对 (C)(E) 有概念，其余几层只知道“存在 + 为什么需要”，细节待补：

- **(A) 网关/接口限流**：令牌桶 / 漏桶 / Sentinel / Nginx limit_req —— 在最外层挡掉或排队超额流量。
- **(B) 本地缓存（多级缓存）**：JVM 内的 Caffeine 等，在进程内先挡一层，再到 Redis。
- **(D) 连接池调优 / 读写分离 / 分库分表**：单池大小要按机器数全局算账（`N台 × 单池`）；HikariCP 经验公式 `连接数 ≈ CPU核数×2 + 磁盘数`；ProxySQL/MyCat 做连接收敛。
- **熔断降级**：DB/Redis 扛不住时返回兜底数据，牺牲部分功能保整体不雪崩（V4 雪崩会带一点思路）。

> 计划：这些各层留到 V1~V7 学完后，再回头单独深挖。本专题主要打透 **(C) Redis 缓存** 这条主线。

### 2. 缓存一致性：缓存还在、但 DB 数据真的改了怎么办

- V2 现状：**只有读路径，没有写路径**。DB 改了之后，缓存会**脏到 TTL 自然过期为止**（我们设 60s）→ 这叫「最终一致」，最长脏 60 秒。
- 业界标准更快做法：在**更新数据的地方**加一步「更新 DB + **删除**缓存」（删而非改：懒加载更省、避免并发写覆盖）。
- 为什么没那么简单：先删缓存还是先更 DB、删完后并发请求又把旧值读回回填……会引出**延迟双删、先更DB再删缓存、订阅 binlog(Canal)** 等一堆方案。
- 定位：这是缓存的“原罪”之一，是个能单开专题的大话题，**不在 穿透/击穿/雪崩 主线上**，等主线走完再深挖。

### 3. 学完本专题后，这几层我能懂多少（预期）

| 层 | 学完后掌握程度 |
|----|--------------|
| Redis 缓存 (C)、Redis 自身可靠性、分布式锁 | ✅ 彻底懂（V2–V7 主线） |
| DB 连接池/瓶颈 (E) | ✅ 懂原理（V1） |
| 限流(A) / 本地缓存(B) / 读写分离分库分表(D) / 熔断降级 | 🟡 知道它存在、为什么需要，但不深入 |

→ 结论：缓存这根主线会很扎实；外层的限流、网关、分库分表会从“没听过”变成“知道是干嘛的”，足够看懂整条链路骨架。剩下每层都能再开一个专题深挖。
